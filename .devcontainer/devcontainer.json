{
    "name": "LaTeX Build",
    "dockerFile": "Dockerfile",
    "runArgs": ["--privileged"],
    "customizations": {
        "vscode": {
            "extensions": [
            ]
        }
    }
}